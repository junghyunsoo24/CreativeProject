<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.CAOMapper">
    <select id="selectAll" resultType="DB.DTO.ConsumptionAmountOutsiderDTO">
        SELECT *
        FROM consumption_amount_outsider
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="DB.DTO.ConsumptionAmountOutsiderDTO">
        INSERT INTO consumption_amount(year, month, dong_code, dong_name, industry_code, industry_name, residential_city, amount)
        VALUES (#{year}, #{month}, #{dong_code}, #{dong_name}, #{industry_code}, #{industry_name}, #{residential_city}, #{amount})
    </insert>
</mapper>
