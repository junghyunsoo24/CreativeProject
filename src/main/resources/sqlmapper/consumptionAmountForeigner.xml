<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.CAFMapper">
    <select id="selectAll" resultType="DB.DTO.ConsumptionAmountForeignerDTO">
        SELECT *
        FROM consumption_amount_foreigner
    </select>

    <insert id="insertAll" useGeneratedKeys="true" keyProperty="id" parameterType="DB.DTO.ConsumptionAmountForeignerDTO">
        INSERT INTO consumption_amount(year, month, metropolitan_code, city_code, dong_code, dong_name, industry_code, industry_name, natinality, period, amount, consumption_amount_foreigner)
        VALUES (#{year}, #{month}, #{metropolitan_code}, #{city_code}, #{dong_code}, #{dong_name}, #{industry_code}, #{industry_name}, #{nationality}, #{period}, #{count}, #{amount}, #{consumptionAmountForeigner})
    </insert>
</mapper>
